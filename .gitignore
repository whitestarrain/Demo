# Ignore all
*

# Unignore all with extensions
!*.*

# Unignore all dirs
!*/

### Above combination will ignore all files without extension ###

# Ignore files with extension `.class` & `.sm`
*.class
*.sm

# Ignore files with extension .o .a .so .so.* .d
*.o
*.a
*.so
*.so.*
*.d

# Ignore `bin` dir
bin/
# or
*/bin/*

# Unignore all `.jar` in `bin` dir
!*/bin/*.jar

# Ignore all `library.jar` in `bin` dir
*/bin/library.jar

!**/Makefile

*.elc
.vscode
output

temp
worknode_scheduling/.settings
worknode_scheduling/.project
worknode_scheduling/.classpath
**/target/
